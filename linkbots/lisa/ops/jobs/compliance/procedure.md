# Personal compliance procedure

Source-only and default-disabled. The hourly heartbeat evaluates a pure battery intent; it never delivers directly. Selfie and battery prompts are bundled into scheduled source messages.
