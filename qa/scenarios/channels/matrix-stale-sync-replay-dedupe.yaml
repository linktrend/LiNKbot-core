title: Matrix stale sync replay is absorbed by inbound dedupe
scenario:
  id: matrix-stale-sync-replay-dedupe
  surface: channels
  coverage:
    primary:
      - channels.reconnect
  execution:
    profiles: { "matrix:all": 32, "matrix:transport": 31 }
    kind: flow
    channel: matrix
    timeoutMs: 90000
    retryCount: 0
    config:
      matrixTopology:
        defaultRoomKey: main
        rooms:
          - encrypted: false
            key: "${ltfx.n.606b6d2235c57d233d35.v1}"
            kind: group
            members:
              - driver
              - observer
              - sut
            name: Matrix QA Stale Sync Room
            requireMention: true

flow:
  module: ./live-transports/matrix/scenarios/scenario-runtime-restart.js
  call: runStaleSyncReplayDedupeScenario
  args:
    - expr: "scenarioContext"
